<Page x:Class="HospitalD.EditMedicalRecordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HospitalD"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EditMedicalRecordPage" Background="#B2EBF2">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Информация о записи -->
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Редактирование медицинской записи" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock x:Name="RecordInfoText" FontSize="14"/>
        </StackPanel>

        <!-- Форма редактирования -->
        <StackPanel Grid.Row="1" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Диагноз:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,5"/>
                <ComboBox x:Name="DiagnosisComboBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"
                          DisplayMemberPath="Name" SelectedValuePath="ID_Diagnosis"/>

                <TextBlock Text="Процедура:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,5"/>
                <ComboBox x:Name="ProcedureComboBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"
                          DisplayMemberPath="Name" SelectedValuePath="ID_Procedure"/>

                <TextBlock Text="Лекарство:" Grid.Row="2" Grid.Column="0" Margin="0,0,10,5"/>
                <ComboBox x:Name="MedicationComboBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5"
                          DisplayMemberPath="Name" SelectedValuePath="ID_Medication"/>

                <TextBlock Text="Отделение:" Grid.Row="3" Grid.Column="0" Margin="0,0,10,5"/>
                <ComboBox x:Name="DepartmentComboBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5"
                          DisplayMemberPath="Name" SelectedValuePath="ID_Department"/>

                <TextBlock Text="Дата выписки:" Grid.Row="4" Grid.Column="0" Margin="0,0,10,5"/>
                <DatePicker x:Name="DischargeDatePicker" Grid.Row="4" Grid.Column="1" Margin="0,0,0,5"/>

                <TextBlock Text="Примечания:" Grid.Row="5" Grid.Column="0" Margin="0,0,10,5"/>
                <TextBox x:Name="NotesTextBox" Grid.Row="5" Grid.Column="1" Margin="0,0,0,5" 
                         Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
            </Grid>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="Сохранить" Click="SaveButton_Click" Width="100" Margin="0,0,10,0" Background="Aqua"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Page>