<Page x:Class="HospitalD.EmployeeMedicalRecordsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HospitalD"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EmployeeMedicalRecordsPage" Background="#B2EBF2">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель фильтрации -->
        <WrapPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Пациент:" Margin="0,0,10,0"/>
                <ComboBox x:Name="PatientComboBox" Width="200" Margin="0,0,10,0"
                          DisplayMemberPath="FullName" SelectedValuePath="ID_Patient"/>
                <TextBlock Text="Фильтр по дате:" Margin="0,0,10,0"/>
                <DatePicker x:Name="DateFromFilter" Margin="0,0,10,0"/>
                <TextBlock Text="по" Margin="0,0,10,0"/>
                <DatePicker x:Name="DateToFilter" Margin="0,0,10,0"/>
                <Button Content="Применить" Click="ApplyFilter_Click" Margin="0,0,10,0"/>
                <Button Content="Сбросить" Click="ResetFilter_Click"/>
            </StackPanel>
        </WrapPanel>

        <!-- Таблица данных -->
        <DataGrid Grid.Row="1" Name="MedicalRecordsDataGrid" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата записи" Binding="{Binding RecordDate, StringFormat={}{0:dd.MM.yyyy}}" Width="*"/>
                <DataGridTextColumn Header="Пациент" Binding="{Binding Patient.FullName}" Width="*"/>
                <DataGridTextColumn Header="Диагноз" Binding="{Binding Diagnosis.Name}" Width="*"/>
                <DataGridTextColumn Header="Процедура" Binding="{Binding MedicalProcedure.Name}" Width="*"/>
                <DataGridTextColumn Header="Лекарство" Binding="{Binding Medication.Name}" Width="*"/>
                <DataGridTextColumn Header="Врач" Binding="{Binding Staff.FullName}" Width="*"/>
                <DataGridTextColumn Header="Примечания" Binding="{Binding Notes}" Width="*"/>
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Изменить" Click="EditRecord_Click" 
                            HorizontalAlignment="Left" Width="80"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>