<Page x:Class="HospitalD.AssignTreatmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HospitalD"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AssignTreatmentPage" Background="#B2EBF2">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Форма назначения лечения -->
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Назначение лечения" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Пациент:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,5"/>
                <ComboBox x:Name="PatientComboBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"
                          DisplayMemberPath="FullName" SelectedValuePath="ID_Patient"/>

                <TextBlock Text="Диагноз:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,5"/>
                <ComboBox x:Name="DiagnosisComboBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"
                          DisplayMemberPath="Name" SelectedValuePath="ID_Diagnosis"/>

                <TextBlock Text="Процедура:" Grid.Row="2" Grid.Column="0" Margin="0,0,10,5"/>
                <ComboBox x:Name="ProcedureComboBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5"
                          DisplayMemberPath="Name" SelectedValuePath="ID_Procedure"/>

                <TextBlock Text="Лекарство:" Grid.Row="3" Grid.Column="0" Margin="0,0,10,5"/>
                <ComboBox x:Name="MedicationComboBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5"
                          DisplayMemberPath="Name" SelectedValuePath="ID_Medication"/>

               

                <TextBlock Text="Дата назначения:" Grid.Row="5" Grid.Column="0" Margin="0,0,10,5"/>
                <DatePicker x:Name="RecordDatePicker" Grid.Row="5" Grid.Column="1" Margin="0,0,0,5"/>

                <TextBlock Text="Примечания:" Grid.Row="6" Grid.Column="0" Margin="0,0,10,5"/>
                <TextBox x:Name="NotesTextBox" Grid.Row="6" Grid.Column="1" Margin="0,0,0,5" 
                         Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
            </Grid>
        </StackPanel>

        <!-- Кнопка сохранения -->
        <Button Grid.Row="2" Content="Сохранить назначение" HorizontalAlignment="Center" Margin="0,10"
                Click="SaveTreatment_Click" Width="180" Background="Aqua"/>
    </Grid>
</Page>